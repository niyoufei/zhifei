# ZDoc 本地启动器静态骨架

本目录仅为本地启动器最小静态骨架。

当前边界：

1. 当前不启动服务。
2. 当前不访问 endpoint。
3. 当前不执行 Ollama 命令。
4. 当前不读取真实 KG。
5. 当前不读取真实项目资料。
6. 当前不触发 generation/export/write-back。
7. 当前不进入 trial。
8. 当前不创建真正 App 安装包。

如需真实启动控制，必须另设授权节点，并在授权节点中重新声明允许范围、禁止范围和阻断条件。
