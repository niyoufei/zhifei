# KG-RUNTIME-28: ZDoc KG Adapter Contract Mapping Design and Authorization Gate

## 1. Step Identity

- Step: KG-RUNTIME-28.
- Name: ZDoc KG adapter contract mapping design and authorization gate.
- Nature: docs-only adapter contract mapping design and authorization gate.
- Repository: `/Users/youfeini/Desktop/文档生成系统`.
- Branch: `main`.
- Start HEAD: `65705b40cb709b69e450505302d0b542786a28f1`.
- Start tag: `v0.1.408-zdoc-kg-safe-structural-contract-frozen-audit-gate`.

## 2. KG-RUNTIME-27 Review Conclusion Summary

KG-RUNTIME-27 completed only a docs-only frozen audit package and adapter contract mapping authorization gate.

KG-RUNTIME-27 confirmed that the current unique authorization target remains:

- `知识图谱/ZF-KG-12-Municipal-Bridge.json`

KG-RUNTIME-27 did not read real KG file body content.

KG-RUNTIME-27 did not parse real KG JSON.

KG-RUNTIME-27 did not modify `backend/kg_read_only_preview_adapter.py`.

KG-RUNTIME-27 did not modify any route, `main.py`, or runtime code.

KG-RUNTIME-27 did not connect RAG, prompt registry, or system instruction registry.

KG-RUNTIME-27 did not enter real knowledge graph use.

KG-RUNTIME-27 set KG-RUNTIME-28 as a separate explicit authorization gate. KG-RUNTIME-28 may only be docs-only mapping design or authorization gate work, with no direct adapter modification and no direct real KG integration.

## 3. KG-RUNTIME-26 Safe Structural Contract Result Frozen Record

KG-RUNTIME-26 completed safe structural contract read-only smoke validation successfully.

- Safe structural contract executed successfully: yes.
- JSON parse success: yes.
- `parse_ok=true`.
- Top-level type: `dict`.
- Contract scope: `top-level plus first/second structural levels only`.
- Module contract count: `44`.
- Adapter structural path whitelist count: `69/180`.

KG-RUNTIME-26 output range was limited to:

- structure paths;
- types;
- counts;
- field type sets;
- structural judgment basis.

KG-RUNTIME-26 did not output real business body values.

KG-RUNTIME-26 did not output entity body content.

KG-RUNTIME-26 did not output knowledge-entry body content.

KG-RUNTIME-26 did not output prompt content.

KG-RUNTIME-26 did not output system instruction content.

KG-RUNTIME-26 did not output evidence content.

KG-RUNTIME-26 did not output scoring content.

The KG-RUNTIME-26 result must not be used as evidence.

The KG-RUNTIME-26 result must not be used as scoring.

## 4. Current Unique Authorized Target File

The current unique authorized target file remains:

- `知识图谱/ZF-KG-12-Municipal-Bridge.json`

This KG-RUNTIME-28 step did not read this file body content.

This KG-RUNTIME-28 step did not parse this file as JSON.

This KG-RUNTIME-28 step did not execute, load, register, enable, or connect this file to any adapter, route, registry, service, RAG path, prompt registry, system instruction registry, generation path, export path, ZBid writeback path, test path, frontend path, config path, or model path.

## 5. Current Verified Scope

The verified scope remains structural only:

- KG-RUNTIME-20 metadata-level read-only smoke for the single target path.
- KG-RUNTIME-22 JSON structure-level read-only smoke for the single target path.
- KG-RUNTIME-24 nested-structure-profile read-only smoke for the single target path.
- KG-RUNTIME-26 safe structural contract read-only smoke for the single target path.
- KG-RUNTIME-27 docs-only frozen audit and KG-RUNTIME-28 authorization gate.
- File-level metadata from prior authorized metadata review.
- JSON parse success from prior authorized structure reviews.
- Top-level JSON type: `dict`.
- Safe structural contract scope: `top-level plus first/second structural levels only`.
- Module contract count: `44`.
- Adapter structural path whitelist count: `69/180`.
- Structure paths, types, counts, field type sets, and structural judgment basis only.

## 6. Current Unverified Scope

The following remain unverified:

- real business meaning;
- real business body values;
- entity body correctness;
- knowledge-entry body correctness;
- prompt content correctness;
- system instruction content correctness;
- evidence behavior;
- scoring behavior;
- runtime loading behavior;
- registry behavior;
- adapter behavior;
- route behavior;
- RAG behavior;
- prompt registry behavior;
- system instruction registry behavior;
- generation behavior;
- export behavior;
- ZBid writeback behavior;
- Ollama behavior;
- model behavior;
- any running system consumption of the KG file.

## 7. Why Real Use Is Still Not Authorized

The project still cannot enter real KG use because the current record is structural only.

KG-RUNTIME-26 verified parseability and limited structure shape only. It did not validate business content, entity content, knowledge-entry content, prompt content, system instruction content, evidence content, scoring content, runtime integration, registry behavior, RAG behavior, adapter behavior, route behavior, generation behavior, export behavior, or ZBid writeback behavior.

No real knowledge package has been loaded.

No real registry has been created.

No knowledge package has been registered, enabled, or loaded.

No service has been run.

No endpoint has been accessed.

No Ollama or model path has been used.

## 8. Why Adapter Modification Is Still Not Authorized

The project still cannot directly modify `backend/kg_read_only_preview_adapter.py` because the only frozen result available for adapter planning is a structure-only contract summary.

The current record defines a design boundary, not an implementation.

The current record does not authorize real KG value reads, route wiring, service behavior, RAG input, prompt registry input, system instruction registry input, evidence extraction, scoring extraction, generation input, export input, or ZBid writeback input.

Any later implementation must first pass a separate frozen audit package and controlled implementation authorization gate.

## 9. Adapter Contract Mapping Design Target

The adapter contract mapping target is to define a future-safe boundary for a possible adapter contract mapping step without implementing it.

The design must preserve:

- default-off behavior;
- manual-trigger behavior;
- read-only behavior;
- no-write behavior;
- no-evidence behavior;
- no-scoring behavior;
- no-RAG behavior;
- no-generation behavior;
- no-export behavior;
- no-ZBid-writeback behavior;
- no-Ollama behavior;
- no-model-upgrade behavior;
- no-service-auto-run behavior;
- no-CI-auto-run behavior.

This design does not authorize adapter code changes.

This design does not authorize route code changes.

This design does not authorize `main.py` changes.

This design does not authorize system integration.

## 10. Adapter Readable Structural Path Whitelist Design

A future adapter, if separately authorized, may only read structural paths derived from the KG-RUNTIME-26 structural path whitelist.

The KG-RUNTIME-26 structural path whitelist is the maximum design source for adapter-readable paths.

The whitelist may only be used for structure reads.

The whitelist may only support structure-level contract reporting, such as path presence, type, count, and field type set summaries.

The whitelist must not include business body value output.

The whitelist must not authorize entity body value output.

The whitelist must not authorize knowledge-entry body value output.

The whitelist must not authorize prompt content output.

The whitelist must not authorize system instruction content output.

The whitelist must not authorize evidence content output.

The whitelist must not authorize scoring content output.

The whitelist must not be converted directly into RAG input.

The whitelist must not be used as evidence.

The whitelist must not be used as scoring.

Any path not included in the KG-RUNTIME-26 whitelist must be blocked by default.

## 11. Adapter Forbidden Path Design

A future adapter, if separately authorized, must block:

- prompt content paths;
- system instruction content paths;
- evidence content paths;
- scoring content paths;
- body-generation material value paths;
- entity body paths;
- knowledge-entry body paths;
- any path not included in the KG-RUNTIME-26 structural path whitelist.

The block policy must be default-deny.

The block policy must treat unknown paths as forbidden.

The block policy must not infer permission from business semantics.

The block policy must not infer permission from runtime demand.

The block policy must not allow route, RAG, prompt registry, system instruction registry, generation, export, evidence, scoring, or ZBid writeback consumers to request value reads.

## 12. Adapter Output Field Whitelist Design

A future adapter, if separately authorized, may output at most these fields:

- `ok`
- `enabled`
- `status`
- `reason`
- `source`
- `contract_scope`
- `module_contract_count`
- `adapter_structural_path_whitelist_count`
- `allowed_path_count`
- `blocked_path_count`
- `value_output_policy`
- `no_write`
- `no_evidence`
- `no_scoring`
- `no_rag`
- `no_generation`
- `no_export`
- `no_zbid_writeback`

No other output field is authorized by this design.

No real business body value may be output through any field.

No entity body content may be output through any field.

No knowledge-entry body content may be output through any field.

No prompt content may be output through any field.

No system instruction content may be output through any field.

No evidence content may be output through any field.

No scoring content may be output through any field.

## 13. Adapter Forbidden Output Content Design

A future adapter, if separately authorized, must not output:

- entity body content;
- knowledge-entry body content;
- prompt content;
- system instruction content;
- evidence content;
- scoring content;
- generated document body content;
- any content directly usable as `/generate` input;
- any text block directly usable as RAG input;
- any text block directly usable as prompt registry content;
- any text block directly usable as system instruction registry content;
- any business body value from the real KG file.

Allowed output must remain contract metadata only.

Allowed output must remain structure boundary metadata only.

## 14. Adapter Default-Off and Manual-Trigger Constraints

A future adapter mapping implementation, if separately authorized, must be default-off.

A future adapter mapping implementation, if separately authorized, must be manual-trigger only.

No service startup may auto-run the adapter mapping.

No import side effect may run the adapter mapping.

No CI job may auto-run the adapter mapping.

No route may call the mapping unless a later step explicitly authorizes route integration.

No endpoint may expose the mapping unless a later step explicitly authorizes endpoint exposure.

## 15. Adapter No-Write, No-Evidence, and No-Scoring Constraints

A future adapter mapping implementation, if separately authorized, must be read-only.

A future adapter mapping implementation, if separately authorized, must be no-write.

It must not write document body content.

It must not write `output/job/export`.

It must not generate DOCX.

It must not write a registry.

It must not write a knowledge package.

It must not write ZBid data.

It must not produce evidence.

It must not be used as evidence.

It must not produce scoring.

It must not be used as scoring.

KG-RUNTIME-26 and KG-RUNTIME-27 results must not be used as evidence.

KG-RUNTIME-26 and KG-RUNTIME-27 results must not be used as scoring.

## 16. Adapter No-RAG, No-Generation, No-Export, and No-ZBid-Writeback Constraints

A future adapter mapping implementation, if separately authorized, must not connect to RAG.

It must not produce RAG chunks.

It must not produce retrieval input.

It must not connect to `/generate`.

It must not produce generation-ready text.

It must not connect to `/export_docx`.

It must not produce export-ready content.

It must not connect to `/review/apply`.

It must not trigger ZBid writeback.

It must not create or update any business artifact.

## 17. Adapter No-Ollama and No-Model-Upgrade Constraints

A future adapter mapping implementation, if separately authorized, must not run Ollama.

It must not call a local model.

It must not call a remote model.

It must not upgrade, pull, delete, replace, or configure any model.

It must not require model availability for adapter contract mapping.

## 18. Later Implementation Authorization Conditions

KG-RUNTIME-29, if separately authorized, may only be:

- adapter contract mapping frozen audit package and controlled implementation authorization gate docs-only.

KG-RUNTIME-29 must not directly modify `backend/kg_read_only_preview_adapter.py`.

KG-RUNTIME-29 must not directly modify route code.

KG-RUNTIME-29 must not directly modify `main.py`.

KG-RUNTIME-29 must not directly connect the real KG file.

KG-RUNTIME-29 must not implement runtime behavior.

KG-RUNTIME-29 must not run a service.

KG-RUNTIME-29 must not access a port.

KG-RUNTIME-29 must not enter real KG use.

KG-RUNTIME-29 must require a separate explicit instruction and must not be entered automatically by KG-RUNTIME-28.

## 19. KG-RUNTIME-28 Negative Execution Confirmation

This KG-RUNTIME-28 step did not read real KG file body content.

This KG-RUNTIME-28 step did not open `知识图谱/ZF-KG-12-Municipal-Bridge.json` content.

This KG-RUNTIME-28 step did not parse real KG JSON.

This KG-RUNTIME-28 step did not run `python3 -m json.tool`.

This KG-RUNTIME-28 step did not run any Python script to read real KG JSON.

This KG-RUNTIME-28 step did not read `AI知识图谱大全` content.

This KG-RUNTIME-28 step did not copy, move, or delete `AI知识图谱大全`.

This KG-RUNTIME-28 step did not load a real knowledge package.

This KG-RUNTIME-28 step did not create a real registry.

This KG-RUNTIME-28 step did not register, enable, or load a knowledge package.

This KG-RUNTIME-28 step did not run a service.

This KG-RUNTIME-28 step did not access a port.

This KG-RUNTIME-28 step did not call `/health`.

This KG-RUNTIME-28 step did not call `/kg/read-only-preview`.

This KG-RUNTIME-28 step did not trigger `/generate`, `/export_docx`, or `/review/apply`.

This KG-RUNTIME-28 step did not trigger ZBid writeback.

This KG-RUNTIME-28 step did not write document body content.

This KG-RUNTIME-28 step did not write `output/job/export`.

This KG-RUNTIME-28 step did not generate DOCX.

This KG-RUNTIME-28 step did not run Ollama.

This KG-RUNTIME-28 step did not upgrade or pull a model.

This KG-RUNTIME-28 step did not delete or replace a model.

This KG-RUNTIME-28 step did not modify code, JSON, tests, frontend, or config.

This KG-RUNTIME-28 step did not modify `backend/kg_read_only_preview_adapter.py`.

This KG-RUNTIME-28 step did not modify any route, `main.py`, or runtime code.

This KG-RUNTIME-28 step did not connect RAG, prompt registry, or system instruction registry.

This KG-RUNTIME-28 step did not connect tests or CI.

This KG-RUNTIME-28 step did not add `.pyc` or `__pycache__` changes.

This KG-RUNTIME-28 step did not enter real knowledge graph use.

## 20. Validation Results

- `git diff --check`: passed with exit code 0 for the docs-only working tree diff.
- `git diff --cached --check`: passed with exit code 0 after staging only this target docs file.

## 21. Final Boundary Conclusion

KG-RUNTIME-28 is limited to this docs-only adapter contract mapping design and authorization gate.

Only this target docs file is added:

- `docs/zdoc-kg-adapter-contract-mapping-design-and-authorization-gate-kg-runtime-28.md`

No code, JSON, tests, frontend, config, `output/job/export`, `.pyc`, or `__pycache__` change is introduced.

The current unique authorization target remains:

- `知识图谱/ZF-KG-12-Municipal-Bridge.json`

The adapter contract mapping design is complete as a design and authorization gate only.

The KG-RUNTIME-26 and KG-RUNTIME-27 structure results are frozen only as structure-only records and must not be used as evidence or scoring.

KG-RUNTIME-28 does not enter KG-RUNTIME-29.
