# ZDoc Domestic Top-Tier Model Fleet Latest-Version Lookup Authorization Gate - MODEL-FLEET-GOVERNANCE-002

## 1. Node

`MODEL-FLEET-GOVERNANCE-002-LATEST-VERSION-LOOKUP-AUTHORIZATION-GATE`

This node is a docs-only authorization gate for whether a later node may perform official / trusted latest-version lookup for the local domestic top-tier model fleet.

This node only forms the authorization threshold.

This node does not execute latest-version lookup, does not run Ollama, does not execute any Ollama command, does not upgrade, pull, delete, or replace any model, does not modify the `latest` pointer, does not run the ZDoc service, does not access endpoints, does not read or parse real KG, does not trigger generation / export / write-back, does not write `output`, `job`, or `export`, does not generate images, does not call any image generation tool or image model, and does not enter real use or trial.

## 2. Repository State

- Repository: `/Users/youfeini/Desktop/文档生成系统`
- Branch: `main`
- Current node starting HEAD: `fb57803f0dd66e36066c12cf3b3356dd66b4d757`
- Current node starting remote tag record: `v0.1.561-zdoc-model-fleet-and-construction-image-governance-gate`
- Initial `git status --short`: clean

The current node did not live-query the remote tag.

The current node did not execute `git ls-remote`.

The current node did not perform a network check for the starting remote tag.

## 3. Target Docs Read

The following target docs were readable and were read for this docs-only latest-version lookup authorization gate:

1. `docs/zdoc-local-domestic-top-tier-model-fleet-and-construction-image-generation-governance-gate-model-fleet-governance-001.md`
2. `docs/zdoc-preview-only-validation-result-review-and-kg-safety-gate-kg-runtime-170.md`
3. `docs/zdoc-preview-only-technical-validation-execution-record-kg-runtime-169.md`
4. `docs/zdoc-preview-only-technical-validation-authorization-gate-kg-runtime-168.md`
5. `docs/zdoc-preview-only-current-state-and-validation-scope-review-kg-runtime-167.md`
6. `docs/zdoc-preview-only-readiness-authorization-gate-kg-runtime-166.md`

No real KG file body content was read.

No real KG JSON was parsed.

## 4. Current Facts

1. `MODEL-FLEET-GOVERNANCE-001` has completed and passed.
2. The local model fleet governance threshold has been formed.
3. The construction engineering image generation capability governance threshold has been formed.
4. Currently only `qwen3.6:35b` has completed single-model upgrade evidence intake, lightweight stability validation, and preview-only minimal technical validation.
5. Other models may only be recognized as locally present.
6. Other models must not be recognized as latest.
7. DeepSeek may currently only be recognized as `deepseek-r1:32b` locally present; its latest status has not been checked.
8. Construction engineering image generation capability has currently only completed the governance threshold and must not be recognized as already available.
9. Current state remains `NO-GO FOR MODEL UPGRADE`.
10. Current state remains `NO-GO FOR IMAGE GENERATION EXECUTION`.
11. Current state remains `NO-GO FOR REAL USE`.
12. Current state remains `NO-GO FOR TRIAL`.

## 5. Latest-Version Lookup Authorization Scope Candidates

This node forms only future authorization-scope candidates for online latest-version lookup.

This node does not perform any online lookup.

This node does not treat any model as already latest.

### 5.1 Text Model Latest Lookup Candidate Scope

If a later node receives explicit user authorization, it may only then perform online lookup of official / trusted latest-version information for the following model families:

1. `qwen3.6` / `qwen3` model families.
2. `qwen3-next` model family.
3. `qwen3-coder` model family.
4. `deepseek-r1` model family.
5. Other domestic top-tier text model family candidates only after each candidate is itemized inside the authorized scope before lookup.

### 5.2 Image / Multimodal Model Latest Lookup Candidate Scope

If a later node receives explicit user authorization, it may only then perform online lookup of domestic top-tier image / multimodal model candidates, including but not limited to:

1. Image generation models that can be deployed locally or accessed through private deployment.
2. Multimodal models that can support construction procedure diagrams, construction node diagrams, construction-site images, and safety / civilized construction images.
3. Models that can support Chinese construction prompt understanding, construction safety and civilized construction negative-list constraints, and image-text consistency checking.
4. Specific model names and sources must follow the later authorized scope; this node must not identify or assume them on its own.

## 6. Trusted Source Candidates For Later Online Lookup

If a later node receives explicit user authorization, online lookup may use only official / trusted sources.

Candidate sources include:

1. Official model release pages.
2. Official model repositories.
3. Official organization pages.
4. Ollama official model library.
5. Hugging Face official organization pages.
6. GitHub official repository releases / tags.
7. Official technical reports or model cards.
8. Any other source only after user authorization.

This node did not access any of the sources above.

## 7. Future Latest Lookup Authorization Wording

If later online latest-version lookup is needed, explicit user authorization is required first.

Authorization prompt must prominently show:

**是否需要用户授权：需要。**

Future authorization template:

“我明确授权 MODEL-FLEET-GOVERNANCE-003 执行 domestic top-tier model fleet latest-version lookup。授权范围仅限：只读联网核查本机已知模型族及用户指定国内顶级文本 / 图像 / 多模态模型候选的官方 / 可信最新版本信息。禁止运行 Ollama，禁止执行 `ollama list` / `ollama pull` / `ollama run` / `ollama rm` / `ollama serve`，禁止升级、拉取、删除或替换任何模型，禁止运行 ZDoc 服务，禁止访问 endpoint，禁止读取真实 KG，禁止生成图片，禁止进入真实使用 / 试用阶段。完成后必须回报并停止，等待人工审核。”

The future authorization template is not treated as authorization granted by this node.

This node does not authorize `MODEL-FLEET-GOVERNANCE-003`.

## 8. Later Upgrade Governance Principles

1. Latest lookup is not upgrade authorization.
2. Finding a newer version does not authorize `pull`.
3. Each model must receive per-model authorization.
4. Each model must receive stability validation after upgrade.
5. Each model must receive preview-only review after upgrade.
6. Before any image model or multimodal model is connected, construction safety and civilized construction image negative-list rules and image-text consistency checking rules must be completed separately.
7. No model may automatically delete old versions.
8. No model may automatically modify the `latest` pointer.
9. No model may enter formal trial without review.

## 9. Current Decision

Current decision:

`LATEST-VERSION LOOKUP AUTHORIZATION GATE FORMED / NO LOOKUP EXECUTED`

Explicit stop lines:

`NO-GO FOR MODEL UPGRADE / NO-GO FOR IMAGE GENERATION EXECUTION / NO-GO FOR REAL USE / NO-GO FOR TRIAL`

This decision does not authorize latest-version lookup.

This decision does not authorize model upgrade.

This decision does not authorize model pull, replacement, deletion, or `latest` pointer modification.

This decision does not authorize image generation execution.

This decision does not mean construction engineering image generation capability is already available.

This decision does not authorize real use.

This decision does not authorize trial.

This decision does not authorize 1-2 person controlled trial.

This decision does not authorize 2-5 person limited concurrent trial.

This decision does not mean any model is already latest.

This decision does not mean latest lookup has already completed.

This decision does not mean formal trial readiness.

## 10. Next Node Suggestion

Suggested next model-fleet node:

`MODEL-FLEET-GOVERNANCE-003-LATEST-VERSION-LOOKUP-EXECUTION: domestic top-tier model fleet latest-version lookup after explicit user authorization`

This suggested node may only be executed after the user explicitly authorizes it item by item.

This node does not enter the suggested next node.

`KG-RUNTIME-171-KG-SAFETY-AUTHORIZATION-GATE` remains a later KG safety mainline node, but after the model fleet latest lookup authorization threshold is formed, the master controller must decide whether to execute model latest lookup first or return to the KG safety mainline.

## 11. Prohibited Actions Record

- Ran Ollama: no
- Executed any Ollama command: no
- Queried latest model versions online: no
- Accessed any model official site / model repository / GitHub / Hugging Face / Ollama model library: no
- Upgraded, pulled, deleted, or replaced any model: no
- Modified the `latest` pointer: no
- Ran ZDoc service: no
- Accessed endpoint: no
- Read real KG file body content: no
- Parsed real KG JSON: no
- Triggered generation: no
- Triggered export: no
- Triggered write-back: no
- Wrote `output`: no
- Wrote `job`: no
- Wrote `export`: no
- Used real project material: no
- Used real business data: no
- Generated images: no
- Called any image generation tool or image model: no
- Entered real use or trial: no
- Entered 1-2 person controlled trial: no
- Entered 2-5 person limited concurrent trial: no
- Treated this node as models already latest: no
- Treated this node as latest lookup completed: no
- Treated this node as model upgrade authorized: no
- Treated this node as image generation capability already available: no
- Treated this node as formal trial readiness: no
- Performed directory scan again: no
- Modified adapter / route / helper / `main.py`: no
- Modified frontend / tests / config / JSON: no
- Connected RAG / registry / CI: no
- Added `.pyc` / `__pycache__`: no

## 12. Final Status

- `MODEL-FLEET-GOVERNANCE-002-LATEST-VERSION-LOOKUP-AUTHORIZATION-GATE` completed as a docs-only latest-version lookup authorization gate.
- `MODEL-FLEET-GOVERNANCE-001` has completed and passed.
- Local model fleet governance threshold has been formed.
- Construction engineering image generation capability governance threshold has been formed.
- Only `qwen3.6:35b` has completed the recorded single-model upgrade evidence intake, lightweight stability validation, and preview-only minimal technical validation.
- Other models may only be recognized as locally present.
- Other models must not be recognized as latest.
- DeepSeek may currently only be recognized as `deepseek-r1:32b` locally present; latest status has not been checked.
- Construction engineering image generation capability has not been recognized as already available.
- Future latest-version lookup candidate scopes are recorded.
- Future official / trusted source candidates are recorded.
- Future user authorization wording is recorded and is not treated as already authorized.
- Later upgrade governance principles are recorded.
- Current decision: `LATEST-VERSION LOOKUP AUTHORIZATION GATE FORMED / NO LOOKUP EXECUTED`
- Explicit NO-GO: `NO-GO FOR MODEL UPGRADE / NO-GO FOR IMAGE GENERATION EXECUTION / NO-GO FOR REAL USE / NO-GO FOR TRIAL`
- Suggested next node: `MODEL-FLEET-GOVERNANCE-003-LATEST-VERSION-LOOKUP-EXECUTION: domestic top-tier model fleet latest-version lookup after explicit user authorization`
- Later KG safety mainline node: `KG-RUNTIME-171-KG-SAFETY-AUTHORIZATION-GATE`
- The next node was not entered.

MODEL-FLEET-GOVERNANCE-002 stops here and waits for human review.
