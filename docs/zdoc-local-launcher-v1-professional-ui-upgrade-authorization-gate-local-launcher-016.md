# LOCAL-LAUNCHER-016 ZDOC Local App V1 Professional UI Upgrade Authorization Gate

## 1. Node Identification

- Node: `LOCAL-LAUNCHER-016-ZDOC-LOCAL-APP-V1-PROFESSIONAL-UI-UPGRADE-AUTHORIZATION-GATE`
- Scope: docs-only V1 professional static UI upgrade authorization gate.
- Target artifact: `docs/zdoc-local-launcher-v1-professional-ui-upgrade-authorization-gate-local-launcher-016.md`
- Execution boundary: authorization-boundary documentation only; no V1 artifact modification, no page open, no service run, no endpoint access, no Ollama, no trial, and no generation/export/write-back.

## 2. Baseline

- HEAD: `19eed5b7c428c26ab5a39e3551b408be25b058af`
- Tag: `v0.1.651-local-launcher-zdoc-local-app-v1-controlled-start-ui-skeleton-manual-verification-pass-gate`
- Current branch line: `LOCAL-LAUNCHER`
- Current git branch confirmation: `main`
- Current node nature: docs-only V1 professional UI upgrade authorization gate
- Starting worktree status: clean

The required baseline matched before this document was created.

## 3. Purpose

LOCAL-LAUNCHER-016 defines only the authorization boundary for a possible future V1 professional static UI upgrade. It does not modify, run, open, start, stop, access, or repair any runtime component.

Binding purpose limits:

1. LOCAL-LAUNCHER-016 defines only the professional UI upgrade authorization boundary.
2. LOCAL-LAUNCHER-016 does not implement UI changes.
3. LOCAL-LAUNCHER-016 does not run service.
4. LOCAL-LAUNCHER-016 does not open the HTML page.
5. LOCAL-LAUNCHER-016 does not access endpoint.
6. LOCAL-LAUNCHER-016 does not run Ollama.
7. LOCAL-LAUNCHER-016 does not run npm, yarn, pnpm, pip, or dependency tooling.
8. LOCAL-LAUNCHER-016 does not introduce external dependencies.
9. LOCAL-LAUNCHER-016 does not trigger generation, export, or write-back.
10. LOCAL-LAUNCHER-016 does not enter trial.
11. LOCAL-LAUNCHER-016 does not read real KG.
12. LOCAL-LAUNCHER-016 does not read real project materials.
13. LOCAL-LAUNCHER-016 stops after completion.

## 4. Current V1 UI Status

Current V1 status confirmed from the allowed LOCAL-LAUNCHER-013 through LOCAL-LAUNCHER-015 records and the allowed V1 static files:

1. V1 UI skeleton has been created under `local_launcher/v1/`.
2. V1 static audit has passed.
3. User manual viewing has passed.
4. Current V1 is understandable and acceptable for the completed skeleton objective.
5. Current V1 remains a static UI skeleton.
6. Current V1 has no runtime capability.
7. The next step is not runtime; the next possible step is a professional static UI upgrade.

Current V1 state remains:

1. no service startup;
2. no service stop;
3. no endpoint access;
4. no Ollama run;
5. no trial;
6. no generation/export/write-back;
7. no real KG read;
8. no real project material read;
9. no runtime preflight;
10. no controlled start execution.

## 5. Professional UI Upgrade Objective

The professional UI upgrade objective for a future separately authorized LOCAL-LAUNCHER-017 is limited to static interface quality improvement:

1. Form a local console interface closer to a formal product surface.
2. Improve understandability for ordinary users.
3. Improve administrator efficiency when judging current status.
4. Strengthen visibility of the safety boundary.
5. Strengthen authorization-state expression.
6. Strengthen startup-precheck flow expression.
7. Strengthen segmented expression for service status, ports, logs, and configuration.
8. Strengthen prohibited-capability prompts.
9. Reserve a clear UI area for a possible future V1 runtime preflight.
10. Add no runtime capability.

The professional UI upgrade objective does not authorize service lifecycle control, endpoint health checks, Ollama, model calls, generation, export, write-back, real KG read, real project material read, trial, real use, or 50-user formal deployment.

## 6. Allowed Codex Web-Building Capability Boundary

A future LOCAL-LAUNCHER-017 may use Codex web-building capability only for static UI improvement.

Allowed future static UI work:

1. More professional page layout.
2. Left navigation.
3. Top status bar.
4. Status cards.
5. Risk prompt cards.
6. Authorization status cards.
7. Startup-precheck flow area.
8. Service-status dashboard placeholder.
9. Log, port, and configuration placeholder areas.
10. Disabled-button visual improvement.
11. Responsive layout.
12. Clearer Chinese copy.
13. Static interaction styles such as hover and focus visual effects, without runtime logic.
14. Static JSON state field adjustment, while all permission fields must remain `false`.

Prohibited future UI-building methods and capabilities:

1. npm.
2. yarn.
3. pnpm.
4. pip install.
5. React or Vite project.
6. Tauri or Electron project.
7. External UI library.
8. CDN.
9. Remote font.
10. Network request.
11. Endpoint URL.
12. Startup or stop script.
13. Runtime bridge.

## 7. Recommended Professional UI Structure

The recommended future LOCAL-LAUNCHER-017 page structure should include at least:

1. Top brand area: `ZDoc 本地 AI 文档系统控制台`.
2. Current version area: `V1 专业静态控制台`.
3. Current authorization status area: `未授权启动 / 仅静态展示`.
4. Left navigation area:
   - 总览
   - 启动前检查
   - 服务状态
   - 日志与端口
   - 禁止能力
   - 后续授权
5. Main control panel:
   - 系统状态
   - 启动前检查
   - 服务状态占位
   - 端口状态占位
   - 日志状态占位
   - 配置状态占位
6. Right safety rail:
   - 当前禁止事项
   - no-service
   - no-endpoint
   - no-Ollama
   - no-generation/export/write-back
7. Bottom audit bar:
   - HEAD/tag placeholder
   - no-runtime statement
   - next gate placeholder

This structure is only a recommendation for a later static UI implementation gate. LOCAL-LAUNCHER-016 does not enter that implementation.

## 8. Visual Quality Requirements

Future professional static UI quality requirements:

1. Page hierarchy is clear.
2. Chinese expression is professional and concise.
3. Console style is obvious.
4. Status colors are consistent.
5. Disabled buttons are obvious.
6. Risk prompts are visible without implying panic.
7. Ordinary users can understand that startup is currently unavailable.
8. Administrators can understand the later authorization path.
9. The page displays well on common browser wide screens.
10. The page depends on no external resource.

Recommended color semantics:

1. Gray: unauthorized or disabled.
2. Blue: information or placeholder.
3. Yellow: requires review.
4. Red: prohibited or risk.
5. Green: user manual verification passed, but not runtime authorization.

## 9. Future LOCAL-LAUNCHER-017 Allowed File Scope

If a future LOCAL-LAUNCHER-017 is separately authorized, the recommended allowed modification scope is limited to:

```text
local_launcher/v1/index.html
local_launcher/v1/styles.css
local_launcher/v1/README.md
local_launcher/v1/launcher-state.json
```

The recommended single new documentation file is:

```text
docs/zdoc-local-launcher-v1-professional-ui-static-upgrade-implementation-gate-local-launcher-017.md
```

Future LOCAL-LAUNCHER-017 must not add script files, JavaScript files, dependency files, real App packages, backend files, frontend files, config files, or dependency changes.

## 10. Future LOCAL-LAUNCHER-017 Prohibited Scope

Even if LOCAL-LAUNCHER-017 is later separately authorized, it must prohibit:

1. Running service.
2. Stopping service.
3. Opening the HTML page.
4. Accessing endpoint.
5. Executing curl.
6. Running Ollama.
7. Running npm, yarn, pnpm, or pip.
8. Running tests.
9. Creating real startup script.
10. Creating real stop script.
11. Triggering generation, export, or write-back.
12. Writing output, job, or export.
13. Reading real KG.
14. Reading real project materials.
15. Reading registration, metadata, proof, manifest, or sample instances.
16. Modifying backend, frontend, config, or dependency files.
17. Entering trial.
18. Entering real use.
19. Entering 50-user deployment.

Prohibited means no fallback command, no substitute probe, no hidden background action, no browser access by Codex, no runtime inspection, and no continuation into service behavior.

## 11. Professional UI Acceptance Criteria

Future LOCAL-LAUNCHER-017 professional static UI upgrade should be accepted only if all criteria below pass:

1. Page body is primarily Chinese.
2. Page is clearly better than the current skeleton version.
3. Page structure has a formal console feel.
4. Left navigation or equivalent navigation structure is clear.
5. Status cards are clear.
6. Prohibited-capability prompts are clear.
7. All real action buttons remain disabled.
8. JSON permissions remain all `false`.
9. No endpoint URL exists.
10. No network request exists.
11. No external resource exists.
12. No real KG, real project material, or real sensitive path information exists.
13. README is updated as professional static UI documentation.
14. Docs record no-runtime, no-service, no-endpoint, no-Ollama, and no-trial.
15. Completion requires static audit and user manual re-verification.

Any missing criterion blocks acceptance and requires a correction or reauthorization path, not runtime fallback.

## 12. Runtime Separation Statement

Professional UI upgrade remains separate from runtime.

Binding separation rules:

1. Professional UI upgrade is not runtime preflight.
2. Professional UI upgrade is not controlled start.
3. Professional UI upgrade is not endpoint health check.
4. Professional UI upgrade is not service management.
5. Professional UI upgrade is not preview-only.
6. Real runtime must still require a separate node.

LOCAL-LAUNCHER UI nodes cannot silently become runtime nodes. No future static UI upgrade may imply service startup, service stop, endpoint access, Ollama, generation, export, write-back, trial, real use, real KG read, or real project material read.

## 13. Recommended Next Node

Recommended next node, only after ChatGPT master-control review and explicit user authorization:

`LOCAL-LAUNCHER-017-ZDOC-LOCAL-APP-V1-PROFESSIONAL-UI-STATIC-UPGRADE-IMPLEMENTATION-GATE`

Required constraints for that future node:

1. LOCAL-LAUNCHER-017 can only perform static UI upgrade.
2. LOCAL-LAUNCHER-017 must not run service.
3. LOCAL-LAUNCHER-017 must not open the page.
4. LOCAL-LAUNCHER-017 must not access endpoint.
5. LOCAL-LAUNCHER-017 must not run Ollama.
6. LOCAL-LAUNCHER-017 must not run tests.
7. LOCAL-LAUNCHER-017 must not enter trial.
8. LOCAL-LAUNCHER-017 must not trigger generation/export/write-back.
9. LOCAL-LAUNCHER-017 must stop after completion.

This recommendation is not authorization. LOCAL-LAUNCHER-016 does not enter LOCAL-LAUNCHER-017.

## 14. Decision

`LOCAL-LAUNCHER-016 ZDOC LOCAL APP V1 PROFESSIONAL UI UPGRADE AUTHORIZATION GATE COMPLETED / DOCS-ONLY / NO UI MODIFIED / NO SERVICE STARTED / NO ENDPOINT ACCESSED / NO OLLAMA RUN / NO TRIAL EXECUTED / NO GENERATION EXPORT WRITE-BACK TRIGGERED`

This decision is based only on the allowed LOCAL-LAUNCHER governance documents and allowed V1 static files. It does not rely on backend/frontend source, runtime state, endpoint output, Ollama output, real KG, real project material, registration, metadata, proof, manifest, sample, output, job, or export bodies.

## 15. Next Node Boundary

LOCAL-LAUNCHER-016 stops after this document is created, checked, committed, tagged, pushed, and reported.

LOCAL-LAUNCHER-016 must not enter `LOCAL-LAUNCHER-017`.

LOCAL-LAUNCHER-016 must not modify V1 artifacts.

LOCAL-LAUNCHER-016 must not run service.

LOCAL-LAUNCHER-016 must not open the page.

LOCAL-LAUNCHER-016 must not access endpoints, execute curl or HTTP requests, run Ollama, run tests, enter trial, trigger generation, trigger export, trigger write-back, read real KG, read real project materials, or write output/job/export.

After ChatGPT master-control review of this node, a later separately authorized instruction may decide whether to enter a professional static UI upgrade implementation gate.
