# ZDoc Preview-Only Technical Validation Authorization Gate - KG-RUNTIME-168

## 1. Scope

`KG-RUNTIME-168-PREVIEW-ONLY-TECHNICAL-VALIDATION-AUTHORIZATION-GATE` is a docs-only authorization gate for possible later execution-type ZDoc preview-only technical validation.

This node is based on the completed and passed conclusion of `KG-RUNTIME-167-PREVIEW-ONLY-CURRENT-STATE-AND-VALIDATION-SCOPE-REVIEW`.

This node only records the current state, the already available prerequisite evidence, the execution-type preview-only technical validation candidate scope, the prohibited scope, the later user authorization wording, the current decision, and the next-node recommendation.

This node is not an Ollama command node, not a ZDoc service node, not an endpoint node, not a real KG read node, not a KG JSON parsing node, not a generation/export/write-back node, not preview-only technical validation execution, and not real use or trial use.

This node explicitly:

- Does not run Ollama.
- Does not execute any Ollama command.
- Does not run the ZDoc service.
- Does not access endpoints.
- Does not read real KG.
- Does not read real KG file body content.
- Does not parse real KG JSON.
- Does not trigger generation, export, or write-back.
- Does not write `output`, `job`, or `export`.
- Does not modify adapter, route, helper, or `main.py` files.
- Does not modify frontend, tests, config, or JSON files.
- Does not connect RAG, registry, or CI.
- Does not enter real use or trial use.
- Does not enter 1-2 person controlled trial use.
- Does not enter 2-5 person limited concurrent trial use.
- Does not treat this node as execution-type validation authorization.
- Does not treat this node as preview-only already validated.
- Does not treat this node as formal trial readiness.

## 2. Baseline

This node starts from:

- Starting HEAD: `68f1a96a7990d3a033145ba1a0de4c68fa938e9b`
- Starting remote tag record baseline: `v0.1.557-zdoc-preview-only-current-state-validation-scope-review`
- Prior completed node: `KG-RUNTIME-167-PREVIEW-ONLY-CURRENT-STATE-AND-VALIDATION-SCOPE-REVIEW`
- Current node: `KG-RUNTIME-168-PREVIEW-ONLY-TECHNICAL-VALIDATION-AUTHORIZATION-GATE`

The remote tag baseline above is used only as the controller-provided KG-RUNTIME-167 report record baseline.

This node did not live-query the remote tag.

This node did not execute `git ls-remote`.

This node did not perform any network check for the remote tag.

The following target docs were read for this docs-only authorization gate:

1. `docs/zdoc-preview-only-current-state-and-validation-scope-review-kg-runtime-167.md`
2. `docs/zdoc-preview-only-readiness-authorization-gate-kg-runtime-166.md`
3. `docs/zdoc-qwen3-6-35b-stability-evidence-review-and-preview-only-readiness-gate-kg-runtime-165.md`
4. `docs/zdoc-qwen3-6-35b-user-mediated-stability-validation-evidence-intake-kg-runtime-164.md`
5. `docs/zdoc-qwen3-6-35b-post-upgrade-stability-validation-authorization-gate-kg-runtime-164.md`
6. `docs/zdoc-single-model-upgrade-qwen3-6-35b-user-mediated-pull-execution-evidence-intake-kg-runtime-163-pull-user-mediated-evidence-intake.md`

## 3. Current State

`KG-RUNTIME-167-PREVIEW-ONLY-CURRENT-STATE-AND-VALIDATION-SCOPE-REVIEW` has completed and passed.

The current preview-only state and validation scope review has been completed.

The execution-type preview-only technical validation scope boundary has been formed.

The current state has not executed preview-only technical validation.

The current state has not run the ZDoc service.

The current state has not accessed endpoints.

The current state has not read or parsed real KG.

The current state has not triggered generation, export, or write-back.

The current state has not written `output`, `job`, or `export`.

The current state has not entered real use or trial use.

The current state has not entered 1-2 person controlled trial use.

The current state has not entered 2-5 person limited concurrent trial use.

This node only forms an authorization gate for a later possible execution-type preview-only technical validation node.

This node does not authorize execution-type preview-only technical validation.

This node does not mean preview-only has already been validated.

This node does not mean formal trial readiness.

## 4. Available Prerequisite Evidence

The currently available prerequisite evidence is limited to the evidence recorded by the prior docs chain:

1. `qwen3.6:35b` is shown as installed based on user-provided evidence.
2. `qwen3.6:35b` ID is `07d35212591f`.
3. `qwen3.6:35b` SIZE is `23 GB`.
4. User local lightweight stability validation evidence has been received.
5. Preliminary evidence of basic model response capability has been recorded.
6. The output format observation has been recorded:
   `OUTPUT FORMAT OBSERVATION / verbose reasoning-like trace visible before final answer`
7. The preview-only readiness authorization threshold has been formed.
8. The preview-only current state and validation scope have been reviewed.

The evidence above must not be expanded to mean formal trial readiness.

The evidence above must not be expanded to mean KG safe access completion.

The evidence above must not be expanded to mean preview-only technical validation has already executed.

## 5. Execution-Type Preview-Only Technical Validation Candidate Scope

If a later node receives explicit user authorization to execute preview-only technical validation, the validation scope may only include:

1. Confirming git state.
2. Reading the prior target docs.
3. Executing minimal technical validation inside the preview-only / no-write boundary.
4. Verifying whether a preview-only route or interface can be safely called.
5. Verifying whether formal-chain flags remain false.
6. Verifying whether `/generate` is not triggered.
7. Verifying whether `/export_docx` is not triggered.
8. Verifying whether `/review/apply` is not triggered.
9. Verifying whether ZBid write-back is not triggered.
10. Verifying whether `output`, `job`, or `export` is not written.
11. Verifying whether real KG is not read or parsed.
12. Verifying whether preview packet, validation result, blocked reasons, or equivalent read-only results can be returned.
13. Verifying whether the output format observation can be recorded and identified.
14. Verifying whether prompt constraints prevent process-style thinking text from being output.
15. Verifying whether mixed Chinese-English process text does not appear.
16. Verifying whether model output follows the target format boundary.

This candidate scope defines only a possible future execution-type preview-only technical validation boundary.

This candidate scope is not authorization granted by this node.

This candidate scope does not mean preview-only technical validation has already executed.

## 6. Execution-Type Preview-Only Technical Validation Prohibited Scope

Even if a later node receives execution-type validation authorization, the following remain strictly prohibited:

1. No real generation.
2. No formal export.
3. No review-result write-back.
4. No ZBid write-back.
5. No triggering `/generate`.
6. No triggering `/export_docx`.
7. No triggering `/review/apply`.
8. No writing `output`, `job`, or `export`.
9. No reading or parsing real KG.
10. No real project materials.
11. No real business data.
12. No real use or trial use.
13. No 1-2 person controlled trial use.
14. No 2-5 person limited concurrent trial use.
15. No treating preview-only technical validation as formal trial use.
16. No expanding preview-only technical validation results to mean KG safe access completion.
17. No expanding preview-only technical validation results to mean formal deployment readiness.

## 7. Future User Authorization Wording

If a later node needs to enter execution-type preview-only technical validation, explicit user authorization is required first.

Authorization prompt must prominently show:

**是否需要用户授权：需要。**

Future authorization template:

“我明确授权 KG-RUNTIME-169 执行 command-limited preview-only technical validation。授权范围仅限：确认 git 状态、读取前序目标 docs、在 preview-only / no-write 边界内执行最小技术验证，验证 preview-only 路由或接口只读返回、正式链路标志位保持 false、blocked reasons / validation result 可记录。禁止触发 `/generate`、`/export_docx`、`/review/apply`、ZBid 写回；禁止写 output / job / export；禁止读取或解析真实 KG；禁止使用真实项目资料或真实业务数据；禁止进入真实使用 / 试用阶段。完成后必须回报并停止，等待人工审核。”

This future authorization template is not treated as authorization granted by this node.

This node does not authorize execution-type preview-only technical validation.

## 8. Current Decision

Current decision:

`PREVIEW-ONLY TECHNICAL VALIDATION AUTHORIZATION GATE FORMED / NO EXECUTION AUTHORIZED`

This decision also explicitly means:

`NO-GO FOR TRIAL / NO-GO FOR REAL USE / NO-GO FOR ZDOC SERVICE EXECUTION`

This decision does not authorize ZDoc service execution.

This decision does not authorize endpoint access.

This decision does not authorize reading or parsing real KG.

This decision does not authorize generation, export, or write-back.

This decision does not authorize preview-only technical validation execution.

This decision does not authorize real use or trial use.

This decision does not authorize 1-2 person controlled trial use.

This decision does not authorize 2-5 person limited concurrent trial use.

## 9. Next Node Suggestion

Suggested next node:

`KG-RUNTIME-169-PREVIEW-ONLY-TECHNICAL-VALIDATION-EXECUTION: command-limited preview-only technical validation after explicit user authorization`

This next node may be executed only after the user explicitly authorizes it item by item.

This node does not enter the suggested next node.

The suggested next node must not enter real use or trial use.

The suggested next node must not enter 1-2 person controlled trial use.

The suggested next node must not enter 2-5 person limited concurrent trial use.

## 10. Final Status

- KG-RUNTIME-168-PREVIEW-ONLY-TECHNICAL-VALIDATION-AUTHORIZATION-GATE completed as a docs-only authorization gate.
- KG-RUNTIME-167-PREVIEW-ONLY-CURRENT-STATE-AND-VALIDATION-SCOPE-REVIEW has completed and passed.
- Current preview-only state and validation scope review has been completed.
- Execution-type preview-only technical validation scope boundary has been formed.
- Preview-only technical validation has not executed.
- ZDoc service has not run.
- Endpoints have not been accessed.
- Real KG has not been read.
- Real KG JSON has not been parsed.
- Generation, export, and write-back have not been triggered.
- `output`, `job`, and `export` have not been written.
- Real use or trial use has not been entered.
- 1-2 person controlled trial use has not been entered.
- 2-5 person limited concurrent trial use has not been entered.
- Available prerequisite evidence has been recorded.
- The evidence is not expanded to mean formal trial readiness.
- Execution-type preview-only technical validation candidate scope has been recorded.
- Execution-type preview-only technical validation prohibited scope has been recorded.
- Future user authorization wording has been recorded.
- The future authorization template is not treated as already authorized.
- Current decision: `PREVIEW-ONLY TECHNICAL VALIDATION AUTHORIZATION GATE FORMED / NO EXECUTION AUTHORIZED`
- Explicit NO-GO: `NO-GO FOR TRIAL / NO-GO FOR REAL USE / NO-GO FOR ZDOC SERVICE EXECUTION`
- Suggested next node: `KG-RUNTIME-169-PREVIEW-ONLY-TECHNICAL-VALIDATION-EXECUTION: command-limited preview-only technical validation after explicit user authorization`
- The suggested next node requires explicit item-by-item user authorization before execution.
- The next node was not entered.

KG-RUNTIME-168-PREVIEW-ONLY-TECHNICAL-VALIDATION-AUTHORIZATION-GATE stops here and waits for human review.
